<%= form_with url: 'static_pages', local: true, method: 'get' do |f| %>
  <%= f.label 'ID:' %>
  <%= f.text_field :user_id %><br><br>
  <%= f.submit 'Submit' %>
<% end %>

<% if @photos %>
  <% @photos.each do |photo| %>
    <img src=<%=Flickr.url(photo)%> ><br>
  <% end %>
<% end %>